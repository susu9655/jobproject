package job.data.pjlist;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface PjlistMapper {
	
	/*
	 * //태그로 채용공고 검색 public List<CompanyDto> searchListByTag(String tag);
	 * 
	 * //지역으로 채용공고 검색 public List<EmpAccountDto> searchListByaddr(String area,
	 * String addr);
	 * 
	 * //경력으로 채용공고 검색 public List<CompanyDto> searchListByCareer(String empcontent);
	 */
	
	/*
	 * //추천공고 public List<listCategotyDto> listbytag(int num);
	 */
	 
}
