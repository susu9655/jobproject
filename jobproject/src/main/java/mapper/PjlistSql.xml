<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="job.data.pjlist.PjlistMapper">

<!-- 태그 검색 리스트 출력 쿼리 -->
<select id="listbytag" resultType="listCate" parameterType="String">
	select company.empname from company left outer join category on (company.num=category.num) 
	where tag=#{tag1} and tag=#{tag2} and tag=#{tag3};
</select>



</mapper>